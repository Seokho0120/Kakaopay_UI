import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import styled from 'styled-components';

// 메인 그린 컬러 00aa47
export default function MobileAnimation() {
  return <Title>모바일 영상</Title>;
}

const Title = styled.div`
  font-size: 20px;
`;
